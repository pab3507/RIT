The JAR must be in the same folder as the data directory, but not inside it

If double clicking the JAR does not work then you can use the provided scripts. `start.cmd` on Windows and `starh.sh` on macOS and Linux systems

You may quit AFRS by entering `exit;`
